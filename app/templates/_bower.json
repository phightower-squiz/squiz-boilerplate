{
  "name": "<%= _.slugify(name) %>",
  "version": "<%= version %>",
  "private": true,
  "dependencies": {
    <% if (modules.length >= 1) {
        _.each(modules, function (module) {
    %>"<%= module.name %>": "<%= module.repository %>",
    <% })} %>
    <% if (includeBootstrap) { %>
    "bootstrap-sass-official": "*",
    <% } %>
    <% if (includeBourbon) { %>
    "bourbon": "*",
    <% } %>
    "modernizr": "~2.x",
    "jquery": "*",
    "qunit": "~1.x"
  }
}