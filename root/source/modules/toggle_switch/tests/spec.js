test('The plugin has created the toggle switch ui element', function() {
  expect(1);
  equal($('.toggle-switch-ui').length, 1, 'The toggle switcher has created content.');
});

// @todo - this needs more tests to be added